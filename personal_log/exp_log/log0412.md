# 既定问题

## 我需要获得一批benchmark profiling的参数来作为慢模型触发频率的参考，他们包括：

先保留原始固定 8 步调用逻辑，不改变动作输出，只加日志。记录每个 step 的触发候选指标、是否 slow call、任务名、step、最终是否成功。现在
  评估循环已经有 profile_steps，每步会打印 model.last_step_profile，位置在 vla-scripts/evaluate_calvin.py:281；模型侧每步 profile 存在
  vla-scripts/dual_sys_evaluation.py:209。这个是最自然的扩展点。

  建议至少记录这些字段：

  - step_since_slow：距离上次 generalist 调用过去几步。
  - slow_system：本步是否调用 generalist。
  - dp_action_first：specialist 当前输出 chunk 的第 1 个动作。
  - dp_action_chunk_norm：8 步 chunk 的均值/范数/最大范数。
  - ref_action_first 或 ref_action_valid_part：当前用于条件的 generalist ref action。
  - dp_ref_l2：dp_action 与 ref_action 的偏离，最好按动作维度归一化后算。
  - jerk_l2：连续动作二阶差分，建议只对前 6 维末端位姿算，gripper 单独处理。
  - gripper_flip_count：最近窗口内 gripper 符号翻转次数。
  - aggregation_delta：temporal aggregation 前后的差异，即新 dp_action[:,0] 和最终送入环境的 action_prediction 的距离。
  - sample_var：specialist 多次采样动作方差。这个成本最高，先用 K=3 或只在部分 step 上记录。
  - obs_delta / proprio_delta：可选，记录图像 embedding 或 proprio 变化量，辅助判断场景变化。

（codex）

## codex 建议的实验顺序
先把日志按任务和结果分组：

  - success 轨迹 vs fail 轨迹。
  - failure 前最后 10/20 step vs 普通 step。
  - slow call 后第 0 到第 7 步分别统计。
  - 不同 subtask 分别统计，因为推块、开抽屉、按按钮的动作尺度和 gripper 行为会不同。

  看每个指标的 p50/p75/p90/p95/p99，不要只看均值。阈值初始可以从高分位开始，比如 sample_var > p90、dp_ref_l2 > p90、jerk_l2 > p95，再做
  组合规则。

  第三阶段：shadow trigger
  先实现“影子触发器”：每步根据这些指标判断“如果上线会不会触发 generalist”，但不真的改变调用逻辑。这样你能先估计：

  - 会触发多少次。
  - 会不会在正常成功轨迹上过度触发。
  - 失败前是否确实更容易触发。
  - 如果在固定 8 步刷新点选择跳过，会跳过多少次。

  注意：shadow trigger 不能完全预测动态策略效果，因为一旦你真的跳过 generalist，后续动作和观测分布会变；但它能筛掉明显没用的指标。

  第四阶段：小步在线实验
  再真正改变调度。不要一开始无限延长 generalist 间隔，建议做保守网格：

  - baseline：固定 8 步。
  - max_interval=10。
  - max_interval=12。
  - max_interval=16。

  规则可以是：默认不刷新，直到 max_interval 到达；如果任何风险指标超过阈值，就提前刷新。这样即使触发器漏判，也有最大间隔兜底。

  衡量指标不要只看成功率，还要看：

  - 平均成功序列长度。
  - 每个 episode 的 generalist call 数。
  - 控制频率 / model_s。
  - 不同 subtask 的成功率变化。
  - 失败发生在距离上次 slow call 第几步。

  一个代码层面的注意点
  不要继续依赖当前的 % 8 公式来做动态调度。现在 num_cond_actions 在 vla-scripts/dual_sys_evaluation.py:144 由全局 step 取模算出来，和固
  定 schedule 绑死。动态调度时应该显式维护 last_slow_step 和 steps_since_slow，再由它构造剩余 ref action。否则你会很难判断指标变化到底来
  自策略，还是来自 ref action 对齐错误。

  我会采用的实验顺序

  1. 跑固定 8 步 baseline，只记录日志。
  2. 离线画出 sample_var / dp_ref_l2 / jerk / gripper_flip / aggregation_delta 的分布和 failure 相关性。
  3. 做 shadow trigger，筛选 2 到 3 个最有效指标。
  4. 上线 max_interval=10/12 的保守触发器。
  5. 如果成功率基本不掉，再尝试 max_interval=16。
  6. 如果超过 8 步后掉得明显，再考虑只训练一个轻量 trigger/adaptor，而不是先重训 generalist。

codex resume 019d8085-9467-7420-804f-770b719fd037

## 我先尝试获取benchmark profiling



